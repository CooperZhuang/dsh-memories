修复真实端到端跑出来的三个合并缺陷

第一次用真模型跑通「抽取 → 合并」链路，抽取出乎意料地干净（模型自己写了 kind=preference
和 appliesTo），但合并连挂三次，每次都暴露一个只在真实运行里才会出现的问题：

1. 子代理的 JSON schema 用了 type: ["string","null"]。DSH 的 schema 子集只接受单一 type
   字符串，nullable 必须写成 oneOf —— 而 schema 被拒是整轮在启动时就失败，报错只有一句
   "unsupported JSON schema: ..."。现在用 oneOf，并用 harness 自己的 assertObjectJsonSchema
   在测试里钉住，避免再靠真实运行才发现。

2. maxDepth: 0。DSH 的深度从 1 起算（顶层 agent 是 0，它的子代理是 1），所以 0 表示
   「连这个子代理都不允许存在」。改成 1：子代理能跑，孙代理被拒。

3. toolFilter 的 deny 列表是跨平台的（同时写了 bash 和 pwsh、str_replace_editor 和
   edit），而 tools.restrict() 遇到不存在的工具名会 fail loud —— 在任何一个具体部署上，
   列表里总有几项不存在，于是每一轮合并都注定失败。改成先用注册表里的工具名求交集。

还有一个设计缺陷是端到端暴露的：合并是进程级的，而项目作用域是每个工作区一份，原来用
「恰好执行这一轮的那个会话」的工作区去读项目记忆，于是在 A 工作区触发的合并会去合并 B
工作区的记忆并报告成功。现在入队时把工作区根记在任务里（jobs.root），执行时以它为准；
CREATE TABLE IF NOT EXISTS 不会给已存在的表加列，所以同时补了列迁移。
